# Chapter 5: Functions & Modules
Reusable code blocks, parameters, return values, *args, **kwargs, and importing modules.

## Project: Password Generator & Strength Checker
Run: `python password_tool.py`

## What You'll Learn
- `def` creates functions, `return` sends results back
- Default arguments
- `*args` and `**kwargs`
- `import` brings in modules
